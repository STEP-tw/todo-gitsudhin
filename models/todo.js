class Todo {
  constructor(title,description,items) {
    this.title=title;
    this.description=description;
    this.items=items;
  }
  getTitle(){
    return this.title;
  }
  getTitle(){
    return this.title;
  }

}
